name: Keep Render Backend Awake

on:
  schedule:
    - cron: "*/10 * * * *"   # Every 10 minutes

  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Backend
        run: |
          curl -L https://portfolio-chatbot-backend-3e3y.onrender.com/
